// Clean schema definitions - all data is loaded dynamically from the live Centralized C# API & MongoDB Atlas database.

export const initialUsers = [];
export const initialProsumers = [];
export const initialNodes = [];
export const initialBatterySlots = {};
export const initialReservations = [];
export const initialNodeSchedules = [];
